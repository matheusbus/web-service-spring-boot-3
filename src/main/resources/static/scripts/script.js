function salvarUsuario(){
    const user = {
        login : document.getElementById("login").value,
        password
    };

    let login = document.getElementById("login").value;

    document.getElementById("apresenta_login").innerHTML = login;
    
}